apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stardust.fullname" . }}-test-connection"
  labels:
    {{- include "stardust.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stardust.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
