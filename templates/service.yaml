apiVersion: v1
kind: Service
metadata:
  name: {{ include "stardust.fullname" . }}
  labels:
    {{- include "stardust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stardust.selectorLabels" . | nindent 4 }}
